@props(['title', 'text'])

<article class="site-card">
    <div class="site-card__kicker">motivo</div>
    <h3 class="site-card__title">{{ $title }}</h3>
    <p class="site-card__text">{{ $text }}</p>
</article>
